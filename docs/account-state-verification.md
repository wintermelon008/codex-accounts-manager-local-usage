# 账号状态本地验收记录

日期：2026-09-14（北京时间）。本文件按修改轮次记录验收。真实会话实测只对应注明的那次安装包，不能作为后续修改已完成真实接口实测的证明。

## 19:03 用户授权推送：提交前检查

- 用户另行授权将当前本地版本推送至 `wintermelon008/codex-accounts-manager-local-usage` 的 `local-usage-dashboard` 分支；不包括服务器部署、发布新安装包或重启本地插件。
- 本轮不改功能，提交范围为账号可用性与续期能力分离、凭据绑定证据持久化、青蓝／黄色映射、账号绑定钥匙及对应测试、状态机和验收记录。
- 重跑全量 60 个测试文件、590 项测试通过；类型检查通过，代码检查 0 错误、112 项警告；可移植性检查通过。开发版本仍为 `0.1.19-dev`，两份清单版本一致。
- 日志：`/tmp/codex-runs/manager-push-tests/20260914-190301-52310-5a39/`、`/tmp/codex-runs/manager-push-verify/20260914-190303-52440-bda9/`。这些检查不替代前述未完成的实景边界验收。
- 待提交文件已检查常见凭据、私钥及本机用户目录模式；本地凭据、编译产物和安装包不纳入提交。此前各轮次“不推送”的记录保留历史含义。

## 18:06 青蓝与黄色交换：本轮配色验收

- 仅修改卡片、标签和详情页的颜色映射：`refresh_unavailable` 为青蓝色；`refresh_unavailable_unverified`、`unverified` 为黄色。状态机及界面表格同步，历史原始证据保留旧配色并明确标记。
- 不修改账号判定、证据期限、中文文字、钥匙回调或请求频率；不改变其他网络／配额警告。
- 修改前新增回归断言有 6 项失败；修改后全量 60 个文件、590 项测试通过。覆盖当前／非当前／隐藏卡片、两个未知分支、钥匙仍绑定原账号、标签不变，以及卡片与详情页的深浅色调色板声明。
- 类型检查通过，代码检查 0 错误、112 项警告。上述样式测试检查组件和样式声明，不能冒充真实浏览器像素颜色验证。
- 测试：`/tmp/codex-runs/health-color-swap-tests/20260914-175914-43335-beb2/`；检查：`/tmp/codex-runs/health-color-swap-check/20260914-175915-43467-6a6c/`；修改前反例：`/tmp/codex-runs/health-color-swap-before/20260914-175413-42747-66ef/`。
- 18:02:29 已强制覆盖安装 `0.1.19-dev`，唯一注册版本不变；状态记录、健康判定、详情渲染、面板脚本、两份样式和状态机文档共 7 项安装文件与构建一致。状态记录模块摘要仍为换色前的 `57369a34dbb63e6b243c5ff05df17db8eef37eceebba548e96ea5c6abdd8f4ce`，没有改判定逻辑。包 SHA-256：`e25544113bfaf7d941727d71b6b0d92626cc86b0541b9242f16a4a1166914a21`。
- 已安装代码加真实本地记录的只读核对：15 个账号仍为正常 4、可用但续期不可用 1、续期不可用且会话未知 10；不是上游可用性探测。
- 只关闭配额面板标签，再通过状态栏重新打开，没有重启编辑器。18:03 已读到当前卡片的新 `health-usable` 状态类。重开后临时视图恢复默认每页 10 个、隐藏账号不可见；已重新显示隐藏账号并将每页设为 20，以恢复原先一页展示账号的视图。
- 18:06:29 临时展示分组 B 后，原生辅助功能核对全部 **15 张真实卡片**：青蓝对应类 1、黄色未知对应类 10、正常 4；12 张为隐藏账号；11 个“重新授权”按钮全部存在且启用；中文标签与状态类匹配，旧灰色标签 0。检查后分组 B 已恢复隐藏，辅助功能临时开关也恢复原值。证据日志：`/tmp/codex-runs/health-color-swap-all-live-ui/20260914-180626-44976-e0cc/output.log`。此前仅第一页 10 张的记录不作为全部账号验收。
- 本机没有截屏权限；上述实景结果验证节点、状态类、文字及按钮，配色由已安装样式声明核对，不冒称完成像素取色或所有主题／悬停目视验证。详情页新渲染代码已安装，但当前宿主没有重启，详情页实时渲染仍未验收；下次重启加载新代码。
- 本轮没有切账号，没有发起模型／续期／重新认证请求，没有提交或推送。验收后的记录更新保存在仓库；不为追加日志反复重装同一份代码。

以下为换色前各轮次的历史证据，颜色数量保留当时含义，不作为新版配色验收。

## 17:50 换色前实际运行验收：灰色残留问题已验证消除

- 编辑器实际于 17:29:28 激活最新安装包；17:31 读取真实扩展存储，确认 15 个账号均写入本轮检查标记，4 个正常账号的成功证据及原漏判错误均已保存。审计脚本没有替编辑器写库。
- 17:39:30 原生辅助功能逐卡核对全部 15 个真实账号：正常 4、黄色 1、青蓝色 10、旧灰色标签 0。11 个黄／青蓝账号的“重新授权”按钮全部存在且启用，包含隐藏和非当前账号。临时展示分组 B 后已恢复原隐藏筛选；没有改变账号分组或切号池。
- 界面证据为 [account-state-live-ui-evidence.json](account-state-live-ui-evidence.json)，直接来自 `saved-card-container` 节点，不是对话文本、独立模拟窗口或后台映射。系统截屏权限未授予，故不声称机器完成像素配色验收；用户随后回复“我看了，没问题”，作为当前面板人工确认。
- 17:43 实际选中隐藏青蓝账号，青蓝状态类和启用钥匙均保留；取消选择后恢复原样。[选中场景证据](account-state-live-selection-evidence.json)。没有点击钥匙启动 OAuth，也没有执行任何批量操作。
- 原有后台定时任务在 17:37:50–17:37:51 为 3 个非当前账号、17:44:08 为另 1 个非当前账号完成真实续期，4 个新成功证据均保存，账号保持正常。没有靠新增会话或主动续期制造结果；自动配额刷新仍为用户原设置 1 分钟。
- 17:45:32 当前账号仍使用 17:29:02 的真实成功证据（已超过 16 分钟），已安装判定器仍给黄色，实际卡片也仍有黄色状态类；没有在固定十五分钟时降级。
- 18 分钟持续观察已正常结束：17:32:13–17:50:13，1080 秒、37 次采样，退出码 0；分类始终为正常 4、黄色 1、青蓝色 10。黄色账号使用同一份 17:29:02 的证据，结束时该证据已超过 21 分钟，未在十五分钟阈值降级。日志：`/tmp/codex-runs/no-gray-live-persistence/20260914-173213-39627-7637/`。
- **结论范围：本次灰色残留、续期成功衔接、遗漏错误分类、黄／青蓝钥匙显示及本机有效证据不按十五分钟降级，已有上述实景与运行记录支撑。不是所有账号故障和授权边界均已完整实测。**
- 仍未实景覆盖：最新包第二次重启、真实红色失效账号、黄转青蓝的实际凭据到期、OAuth 取消／失败／错账号，以及其他主题／悬停的像素颜色。用户“看了没问题”不被扩张为这些项目均已通过。
- 本轮只做验收并更新证据文档，没有修改业务代码、再次安装、主动切号、主动续期、模型探测、提交或推送。

## 17:25 消除旧灰色分支：代码修复及实账离线核对

- 用户随后截图确认旧包仍存在灰色“可用性待确认”，该项实景验收失败。不能用此前“按设计保留灰色”的解释掩盖缺陷。
- 修复两处根因：旧版完整续期成功记录未衔接当前凭据；`invalid_refresh_token` 未分类为续期不可用。旧证据衔接必须同时核对续期起止时间、凭据签发时间、账号身份和有效期，且只检查一次、绑定摘要，不使用单个旧时间直接判绿。
- 所有未知类别统一为青蓝色并有原账号钥匙。只有明确续期失败才显示“续期不可用”；没有续期结论时说明“会话状态未知（无续期结论）”，不再另设灰色。浅色主题青蓝标签的样式优先级也已修正。
- 修改前回归测试 5 项失败；修复后全量 59 个文件、582 项通过。类型检查与构建检查通过，代码检查 0 错误、112 项警告。
- 17:25 使用**候选构建**加全部 15 个真实本地账号记录进行离线重放：正常 4、黄色 1、青蓝色续期不可用 10、旧灰色 0。恢复正常的是原先 4 个成功续期账号；补识别的错误账号进入青蓝色。没有发送续期或模型请求，没有写入真实账号、凭据或扩展数据库。此时尚不是运行窗口实景验收。
- 全量日志：`/tmp/codex-runs/no-gray-tests/20260914-172351-37541-26bb/`；检查：`/tmp/codex-runs/no-gray-check/20260914-172352-37674-e952/`；修改前失败：`/tmp/codex-runs/no-gray-before/20260914-172108-37023-0ec1/`。
- 17:27 本地强制覆盖安装成功，唯一注册版本 `0.1.19-dev`。安装目录的状态记录、健康映射、分类、详情页、面板脚本、样式及状态机与候选构建一致；安装后的中文资源中旧灰色标签已不存在，青蓝色两种续期说明均存在。
- 已安装代码加真实账号记录再次重放，断言通过：正常 4、黄色 1、青蓝色续期不可用 10；未写真实账号、凭据或扩展数据库。这是安装目录离线演算，非运行窗口实景；日志：`/tmp/codex-runs/no-gray-installed-accounts/20260914-172751-38743-9a6a/`。
- 本轮包 SHA-256：`ac5df37363dbd55852f879fbb4853eddb79654c62a5df019f27f8fdd76c5fc81`。打包：`/tmp/codex-runs/no-gray-package-final/20260914-172625-38389-4853/`；安装：`/tmp/codex-runs/no-gray-install/20260914-172712-38644-12ed/`。
- 已请求用户按原安排手动重启，未替用户重启。红色真实样本、两次重启、真实交互及新版截图仍不能冒称通过。未提交、未推送、未启动任何模型或续期探测。

## 17:06 重启后继续验收：仍未通过完整验收

- 用户已手动重启；实际扩展宿主日志记录 16:53:39 启动、16:53:41 激活本扩展。只观察到这一次真实重启，不能写成“两次重启通过”。日志：编辑器用户日志目录下的 `20260914T165338/window1/exthost/exthost.log`。
- 唯一注册版本仍为 `0.1.19-dev`，安装目录 10 个关键文件与本次构建摘要一致；本轮没有再次修改代码或安装。
- 对全部 15 个真实账号执行已安装判定器的只读核对：10 个青蓝类别、5 个灰色类别，扩展本地存储中有 11 个账号的状态记录。10 个青蓝类别包含当前账号、2 个可见非当前账号、7 个隐藏账号。记录数量不等于正向证据数量；目前没有可用于验证黄色保持、绿色恢复或红色失效的真实样本。
- 当前统计来自已安装代码加本地存储，不是正在运行的面板画面，也不是上游会话请求结果。不能由其证明 15 个账号实际可用或不可用。
- 只读观察于 17:06:07 正常结束：持续 1080 秒、37 次采样、退出码 0。16:53:37 起的 10 青蓝／5 灰色分类保持到结束，实际持续 12 分 30 秒；全程没有有效正向样本。既不能把总观察时长冒充新分类保持时间，也不能声称“黄色超过十五分钟不降级”已实测通过。
- 原生辅助功能读取到了编辑器及状态栏，但未能读到账号面板；截屏失败，浏览器连接不可用。已请用户提供重启后的面板截图。颜色、钥匙及实际面板标签继续保留未验收；没有把对话文本或动作返回成功当作界面验收证据。
- 本轮新增模型探测 0 次、切号 0 次、主动续期 0 次、重新认证 0 次；未重启窗口、未提交、未推送。临时启用的编辑器辅助功能已恢复原值。

## 16:47 青蓝色未知状态与证据持久化：完整验收尚未完成

- 当前规则与状态机见 [account-state-machine.md](account-state-machine.md)，逐项验收状态见 [account-state-acceptance.md](account-state-acceptance.md)。下方旧轮次不代表当前版本已经通过实景验收。
- 新增青蓝色“续期不可用（会话状态未知）”和账号绑定钥匙；黄色仅表示有有效可用证据的续期不可用账号。详情页、浅色、跟随系统浅色、隐藏和选中样式同步。
- 正向证据按访问凭据实际到期时间生效；自然会话与续期证据存入本机扩展存储，重启不清空。新 OAuth 认证完成并保存后也记录正向证据。
- 全量自动化 58 个测试文件、567 项通过；含模拟认证、模拟两次重启和快进时钟，不能视作相应实景已通过。
- 开发版 0.1.19-dev 已强制覆盖安装；关键安装文件与本次构建一致。SHA-256：66ce666c0c7d3ac8068fbdf583bec863151d65ff0fc6e4128ffa1d0232dff379。
- 本轮没有主动续期真实账号、发起模型请求或切换账号；没有修改自动刷新频率；没有提交或推送。
- 用户选择手动重启后通知。目前尚未验收日常面板、两次真实重启、真实经过十五分钟后的有效证据稳定性及真实重新认证交互。

## 13:31 黄色卡片重新认证钥匙：本轮验收

- 只扩展账号卡片钥匙按钮的显示条件：黄色续期警告也提供原有重新认证操作；没有改变健康分类、认证状态机或认证流程。
- 新增 `test/savedAccountReauthorization.test.ts`，检查实际卡片组件生成的节点、钥匙图标、对应账号的点击回调，以及红色、忙碌、忽略、虚拟账号等边界。只模拟翻面状态和回调，不启动真实认证，也不等同于浏览器目视验收。
- 修改前相关断言失败，修改后全量 57 个测试文件、553 项测试通过；类型检查通过，代码检查零错误、97 项警告。
- 已覆盖安装 `0.1.19-dev`，界面脚本及状态机文档摘要与本地构建一致；安装包 SHA-256：`afb48e5cb9704c6264126b3727846a0e7abb1a650e7bec75f6c55f20ac562fc3`。
- 未提交、未推送，未改变用户凭据或启动认证流程。已打开的账号面板需关闭后重新打开，以加载更新的界面脚本。
- 本机测试日志：`/tmp/codex-runs/yellow-reauth-key-tests/20260914-133032-3191-8ef9/output.log`；检查与打包日志分别保存在 `/tmp/codex-runs/yellow-reauth-key-verify/20260914-133033-3329-2478/` 和 `/tmp/codex-runs/yellow-reauth-key-package/20260914-133035-3575-3212/`。

## 13:00 续期成功确认认证可用：本轮验收

- 本地开发版本仍为 `0.1.19-dev`；运行组件协议未变，仍为 15。未提交、未推送。
- 已覆盖安装本轮构建包，SHA-256 为 `215a882453d7380a9df2a25762d85c17d4f795bfc5934b735ce08b26a10c38ea`。
- 先修改回归断言复现问题：原实现中 8 项相关断言失败，包含“续期成功仍待确认”和“续期成功不清除旧认证异常”。修正后全量 56 个测试文件、542 项测试通过。
- 静态检查通过，零错误、97 项警告；打包成功。
- 新增的协调器集成测试使用模拟续期响应，验证保存签发凭据后直接显示正常；不需要运行会话、不需要模型请求，非当前账号同样适用。
- 已安装包离线验收：直接加载安装目录中的状态处理代码，用明确的测试凭据和事件验证“待确认 → 续期成功后正常 → 后续续期不可用时黄色但仍可用 → 后续明确认证失败时红色 → 后续会话成功恢复黄色”。这些是测试事件，**不是真实续期或真实会话结果**。
- 安装目录的 5 个关键文件与构建产物摘要一致：状态记录、健康映射、会话观察、续期协调，以及已同步的状态机和界面表格文档。扩展索引唯一登记版本为 `0.1.19-dev`。
- 本轮未主动续期真实账号，未发模型请求，未切换账号，未重启正在运行的 VS Code 窗口；没有目视验收已打开窗口的卡片。
- 本轮不增加检测按钮、不改变自动刷新频率，也不新增跨重启持久化：新版加载后，**实际续期成功即确认认证可用**；没有任何有效证据的账号仍为待确认，不会将所有账号强行置为正常。

本机日志：

- 全量测试：`/tmp/codex-runs/renewal-confirms-availability-tests/20260914-125901-97886-b2a6/output.log`
- 静态检查：`/tmp/codex-runs/renewal-confirms-availability-verify/20260914-125902-98055-6f9d/output.log`
- 打包：`/tmp/codex-runs/renewal-confirms-availability-package/20260914-125903-98274-86cc/output.log`

## 12:24 首轮验收：历史记录

以下内容只对应首次修改的安装包。当时“只有真实会话才确认可用”的规则已由上面的本轮修改纠正，不能再用作当前行为说明。

### 版本与安装

- 本地开发版本：`0.1.19-dev`，运行组件协议版本：15。
- 工作分支：`local-usage-dashboard`；提交基线：`b9f2f2315b7483e5d1e2d6c64116e3e1b845c146`。
- 修改保留在工作区，未提交、未推送；未安装回退版。
- 安装包：仓库根目录 `codex-accounts-manager-0.1.19-dev.vsix`。
- 安装包 SHA-256：`642b672f5847fbda386e4ebab1822da2925792aeaf48a5e7aa6d5aba088560c2`。
- 已通过 VS Code 的扩展安装命令覆盖安装；扩展索引中该插件唯一登记的版本是 `0.1.19-dev`。
- 安装目录与本地构建产物的 10 项关键文件摘要一致，包括运行包装脚本、状态记录、真实请求观察、续期协调、桥接、工作台、界面脚本及状态机文档。
- 另外将最终安装包中的运行脚本同步到管理器现有运行目录；该文件原为协议 14，同步后为协议 15，摘要与已安装文件一致。没有停止或重启已运行的进程。

### 验证结果

| 验证层级           | 结果                                    | 能证明什么                                               |
| ------------------ | --------------------------------------- | -------------------------------------------------------- |
| 全量自动测试       | 56 个测试文件、533 项测试通过           | 状态组合与回归场景符合断言                               |
| 静态检查           | 类型检查通过；代码检查零错误、92 项警告 | 代码可编译；警告仍存在，不宣称零警告                     |
| 打包和本地安装     | 成功；关键文件摘要一致                  | 安装的是本次开发包                                       |
| 最终安装包真实请求 | 当前账号的一条短会话正常完成            | 安装包能从真实运行组件接收成功证据，并将该账号映射为正常 |

自动回归覆盖：历史配额错误不判红、缺失或本地过期凭据不单独判红、续期与可用性独立组合、旧失败不能覆盖新成功或触发多余续期、凭据和运行环境隔离、额度限制不等于认证失效，以及配额重置时间的秒／毫秒转换。

截图中“访问凭据因续期凭据被撤销而无法续期”的错误原文也作为桥接回归输入。它是模拟服务端错误，不是真实撤销了用户账号。

### 真实请求的范围

- 使用已安装扩展中的桥接、观察处理和状态映射代码，连接真实 Codex 可执行文件与真实上游。
- 使用当前账号，在独立临时目录、独立运行进程中发送一条“只回复 OK”的消息。
- 模型：`gpt-5.6-sol`。最终安装包测试时间：12:24:14–12:24:18。
- 真实会话终态：正常完成；接收的可用性事件：已确认可用；界面状态映射：待确认 → 正常。
- 没有续期保存的凭据，没有切换用户正在运行的账号，没有批量探测其他账号。测试临时目录已清理。
- 本轮共执行两次单账号真实短会话；第二次使用最终覆盖安装包。两次均成功，不是定时任务。

### 本机日志

- 全量测试：`/tmp/codex-runs/account-state-final-regression/20260914-122317-92036-bd60/output.log`
- 静态检查：`/tmp/codex-runs/account-state-installed-final-check/20260914-122623-93035-7b3b/output.log`
- 打包：`/tmp/codex-runs/account-state-final-package/20260914-122318-92169-0730/output.log`
- 最终安装包真实请求：`/tmp/codex-runs/account-state-installed-live-final/20260914-122414-92750-8e5d/output.log`

以上目录中的 `status.json` 保存退出码与完成时间。临时目录可能被系统清理，故本文件保留关键结果。

### 尚未验证的部分

- 没有故意撤销、停用任何真实账号；红色失效及续期失败分支的证据来自自动回归，不是生产账号故障实测。
- 没有遍历所有账号，也没有测试远端 SSH 环境。
- 没有中断当前 VS Code 会话来重启主扩展宿主，因此不宣称当前已打开窗口已加载新版，或卡片已经完成目视验收。真实验收使用的是已安装文件启动的独立进程。
- 打开中的 VS Code 窗口需要重新打开以确保加载新版运行组件。新宿主对没有本机真实会话证据的账号显示“可用性待确认”，不会把历史配额错误当作认证失效。

状态定义与界面表格见 [账号状态机](account-state-machine.md)。复现实测使用仓库中的 `scripts/verify-installed-account-availability.cjs`，每次显式运行会产生一条真实模型请求，不应放进自动刷新任务。
