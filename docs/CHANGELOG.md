# 变更日志

## Unreleased

## 0.1.18-l1（2026-09-02）

- Dashboard 的“删除封禁账号”按钮仅在 Mailbox 已安装并处于可用状态且存在可删除账号时显示，并且只纳入同时满足 `reauthorize` 和 Mailbox 已标记收到 OpenAI `account deactivated` 邮件的账号；未安装或未激活 Mailbox 时按钮隐藏，不会仅凭重新授权状态删除账号。
- “启动额度倒计时”现在对所有套餐只要任一有效的 5h 或长期主额度窗口尚未消耗即可启用；Free/K12 账号恢复 5h 窗口时也会在卡片中显示该窗口，另一个窗口已消耗或已过 freshness margin 不再阻止按钮显示。
- OAuth 始终使用已登记的 `http://localhost:1455/auth/callback`，Remote-SSH 环境在打开浏览器前显式通过 VS Code `asExternalUri()` 建立回调转发；VS Code 映射到 `localhost:1457` 等本机端口时不再因端口不同而提前失败，授权请求和 token exchange 仍使用已登记的回调地址。
- 删除 Mailbox 邮箱时，如果注册助手存在同邮箱的活动 OAuth 会话，会先自动取消该流程，避免已删除邮箱继续占用 OAuth 回调等待。
- Mailbox 注册助手的接码 Key 池改为保存到扩展宿主服务器上的 `0600` 文件；旧版 VS Code SecretStorage 仅作为一次性迁移/恢复来源，重新进入面板或更换客户端后仍可读取同一服务器上的 Key。
- Mailbox 邮箱池、邮箱凭据、详情和注册助手非敏感记录改为以扩展宿主服务器 `globalStorageUri` 下的受限文件为共享权威；旧客户端状态按设备标识一次性合并，密码、手机号、验证码和 OAuth token 不写入注册记录。
- Mailbox 注册助手会自动选择第一个可用接码 Key；Key 选择器最多显示前 5 个可用 Key，其余 Key 仍保留在 Key 池维护区。
- Mailbox 注册助手开始注册时自动复制邮箱；所有按钮补齐鼠标、触摸和键盘按下反馈；不完整或格式异常的手机号不会再自动复制、自动填入或手动复制。
- 设置页移除“分档切号”入口及分档方式选择；分档调度默认关闭，后续仅可通过 config 层控制。等待时间移到“切换策略”下方，作为无感切号的子设置。
- Dashboard 账号分页默认改为每页 `10` 个；自动配额刷新仍独立保持最多处理 `50` 个账号。切换策略与等待时间现在使用同级设置块。
- 修复本地用量统计漏记 subagent 和压缩后的新轮次：有 `task_started` 时按自身任务边界计入所属账号；没有 `trigger_turn`/`task_started` 时识别启动窗口内的密集复制历史，仅回放短任务的真实增量；累计 token 回退时重新建立高水位，并升级用量缓存版本，避免继续读取旧统计。
- 修复无感切号候选排序：继续按 `windowed` 优先于 `reserve`，每类候选改为先按 `Pro > Plus > Free/K12`，同档位按当前 Dashboard 显示的无感切号池顺序选择首个账号，不再受额度、重置时间或最近选中时间影响；Dashboard 后续切换排序时只同步账号 ID 顺序，切换进程无需重复维护排序规则。
- 修复 Dashboard 的“额度刷新时间”排序，改为使用卡片主额度绿条下显示的长期/周额度重置时间，而不是上方 5 小时窗口的重置时间。

## 0.1.16-l2（2026-08-20）

- Dashboard 账号列表底部新增常用分页控件，支持每页 `10`、`20`、`50` 个账号，并可输入页码快速跳转；默认保持每页 `50` 个账号。
- Mailbox 注册助手的会话卡片支持直接删除对应邮箱，顶部“刷新本地状态”右侧新增“清除所有记录”，可一次清理全部注册会话。
- 模型容量错误 `Selected model is at capacity. Please try a different model.` 的 thread 独立恢复等待改为每次随机 `5–8` 秒后发送 `Continue.`；重复容量错误会重新随机等待。
- 修复额度窗口重置后本机账号用量归因停止的问题：runtime 身份就绪后会重试激活，并在 runtime 重启、账号切换和 ChatGPT 路由重新激活后重新同步；状态接口新增 `attributionActive` 和 `attributionFailureReason`，不补算历史未归因事件。
- Dashboard 顶部概览收缩为当前账号邮箱和四个操作按钮；多选批量导出改为卡片兼容的最小数组格式，仅包含 `id`、`email`、三类 token、`created_at` 和 `last_used`。
- 修复超级炸弹车候补锁定启动时无货商品编码、补货切换商品后无法下单的问题；候补现在会先持久化任务，即使目录为空或一次刷新失败也会继续轮询。按网站契约尊重 `purchasable`/`can_buy`，不再因库存字段短暂为 0 或缺失漏掉低价可购商品。失败、退款或取消的历史订单可重新开始候补；状态新增“正在刷新库存”和下一次刷新实时倒计时。目录瞬时网络失败会安全重试一次，连续失败显示可读错误，不会把“候补未启动”与历史订单混淆。BugTeam 面板所有按钮新增按压反馈，网站入口补齐进行中、成功、失败回执，货架档位显示明确选中结果。
- Manager 主 Dashboard 的每个真实 Codex 账号卡片新增“一键复制 Codex 导入凭据”；扩展宿主直接将单账号 `auth_mode: "chatgpt"`、`OPENAI_API_KEY: null`、`tokens`、`last_refresh` 格式的可导入 JSON 写入剪贴板，Token 不进入 Webview，Gateway/手动虚拟账号不显示该入口。
- Manager 受限无感池导入结果新增逐账号脱敏额度摘要，包括套餐、5h/周额度、Credits 余额和入池状态。
- 修复 Manager 启动归因握手晚于活动 turn 时，当前启用账号的本轮窗口 Token 无法归属的问题；身份确认后会立即补记当前活动 thread。
- Manager `0.1.16-local.61`：Mailbox 的 OAuth 导入现在支持由统一停止按钮取消；OAuth 本地回调监听器确认成功绑定后才打开浏览器，端口冲突时不再出现已打开登录页但无法自动回调的误导状态。
- Mailbox 0.2.2：Codex OAuth 导入纳入统一停止按钮；Manager 取消 OAuth 等待后不再弹出二次失败提示；OAuth 回调监听器绑定成功后才打开登录页面，避免端口冲突时误导用户。
- Mailbox 0.2.1：详情面板填满 Webview 并恢复鼠标滚轮滚动；查询完成改为静默状态事件；Codex OAuth 导入期间仍可查询和接收验证码；活动操作支持停止与删除。

## Codex Accounts BugTeam 0.1.1（2026-08-18）

- 面板改为按服务来源分卡，新增超级炸弹车入口、SecretStorage 买家登录、3 秒基础间隔加 0–1 秒随机偏移的本地库存候补、可选金额上下限、持久幂等自动下单和统一 Sub2 入池；金额边界按 `>=` / `<=` 匹配，两项留空时不限制，购买记录显示下单时固化的预计炸车时间。
- 已购账号额度区域统一显示每个账号的套餐、5h/周额度、Credits 余额和入池状态。
- 修复 Webview 模板换行转义错误导致“保存并验证”触发表单跳转后黑屏；新增真实生成脚本执行回归测试，并为保存、刷新、候补和导入按钮增加进行中、成功、失败视觉反馈。
- 修复完成订单在首次导入或无感池启用不完整后停止轮询的问题；现在按 30 秒节流持续重试，并在扩展重启后恢复待处理状态与警告。

## 维护约定

- `announcements.json` 只保留当前版本的更新公告（`releaseVersion` + 新 `id`），发布新版本时旧版本更新说明可直接移除对应项，避免历史版本在弹窗中继续出现。
- 历史版本正文统一写在本文件，按版本追加条目，不做删除。
- 详情可按 `releaseVersion` 反查本次发布公告对应的 `id` 与变更内容。

## 0.1.18（2026-08-24，上游同步）

### 变更内容

- 兼容 Codex 合并到新版 ChatGPT 桌面应用后的自动重启：识别 `/Applications/ChatGPT.app`，并按选中 bundle 的真实可执行路径精确匹配主进程，保留旧版 `Codex.app` 兼容。
- 修复 Free 账户刷新时被历史 Plus / Pro 订阅信息覆盖的问题；当前配额确认是 Free 时会清理旧订阅到期时间，过期付费订阅不会继续作为当前套餐返回。
- Free 或 28 天以上配额窗口统一显示为“每月”/`Monthly`，覆盖 Dashboard、状态栏、详情、切号选择和配额预警；付费 7 天窗口继续显示为“每周”/`Weekly`。
- 扩展启动时从进程环境或 `CODEX_HOME/.env` 读取代理配置，并让扩展网络请求统一使用代理 dispatcher；无效代理配置会明确报错并阻止请求。
- 生产依赖升级到 `undici@6.28.0`，并补充桌面检测、订阅清理、套餐刷新、代理解析和月度标签回归测试。

### 说明

- 本地开发版本为 `0.1.18-dev`；本地发布版本沿用 `0.1.18-lN` 命名。
- 上游公告 ID：`ann-2026-08-codex-tools-0-1-18-chatgpt-free-quota`。

## 0.1.17（2026-07-17，上游同步）

### 变更内容

- 代理配置支持 `HTTP_PROXY`、`HTTPS_PROXY`、`ALL_PROXY` 与 `NO_PROXY`，进程环境优先于 `CODEX_HOME/.env`，空值可以显式覆盖文件配置。
- 代理 URL 仅支持 `http://` 与 `https://`；解析失败或协议不支持时安全阻止扩展网络请求，不静默回退直连。
- 接口缺失 5 小时窗口时，兼容百分比规范化为 `100%`，但继续保持 `hourlyWindowPresent=false`，不影响自动化窗口判断。
- 状态栏中的 5 小时额度跟随 `5 小时配额控制` 设置；关闭时状态栏和 tooltip 只显示每周额度，设置变化立即刷新；原始套餐标识统一显示为易读名称。
- 生产依赖先升级到 `undici@6.27.0`，随后在 `0.1.18` 合并为 `6.28.0`。

### 说明

- 上游公告 ID：`ann-2026-07-codex-tools-0-1-17-proxy-statusbar`。

## 0.1.16-local.60（2026-08-08）

### 本地用量面板

- 用量范围改为可选的六类时间类别：`24h`、`3d`、`7d`、`14d`、`7w`、`7m`。面板最多 8 行：24 小时使用 8 个 3 小时桶，3 天将四个 3 小时桶合并为半天桶，其余类别使用 7 个日/合并日/周/自然月桶。
- 设置新增独立的多选范围面板，全部取消时归一化为 `24h`；切换标签只在 Webview 内投影缓存，不会启动新的 session 扫描。
- 扫描缓存短期保留 3 小时桶，长期保留日桶覆盖范围。首次启用 `7w`/`7m` 并点击“刷新用量”时只回填缺失历史，后续刷新复用日桶并只扫描短期窗口。

### 修复

- 模型容量错误 `Selected model is at capacity. Please try a different model.` 现在按 thread 独立排队，每个会话等待 60 秒后自动发送一次 `Continue.`；重复容量错误会重新进入该会话队列。
- 无感 OAuth 切号优先领取等待中的容量恢复任务，领取后清理原计时器和队列；切号后的新容量错误仍按新的 60 秒周期恢复。Gateway 路由切换不抢占该队列。
- 修复扩展启动时 Gateway adapter 已配置但 ChatGPT 路由未完成 OAuth 注入的问题，避免将 shim 内部 Bearer token 错误转发到 ChatGPT 上游。
- 完成 Gateway 虚拟账号、可选集成隔离以及相关账号卡片、导入器和 Dashboard 的同步修复。

### 验证说明

- 通过 `npm run verify`、`npm test`、`npm run audit:integration-portability` 和 `npm run package`。

## 0.1.16-local.59（2026-08-06）

### 发布修复

- 将当前可复现核心 Manager 构建版本提升为 `0.1.16-local.59`，并同步本地构建内容，使干净 checkout 可以直接运行 `npm run package`。
- 核心 VSIX 不包含飞书导入器或 Sub2API 可选包。

### 验证说明

- `npm run package`、40 个测试文件和 334 项测试全部通过。

## 0.1.16-local.55（2026-08-03）

### 修复

- 修复“启动额度倒计时”按钮错误排除 30 天等非 5 小时/7 天主窗口的问题。显示条件现在以服务端报告的实际窗口时长减五分钟为准；例如 30 天窗口在剩余时间低于 `29d23h55m` 时隐藏。
- 将窗口时长随 Dashboard 状态传入卡片，使面板保持打开时也能按 30 天等实际窗口时长动态隐藏按钮。

### 验证说明

- 覆盖仅有 30 天窗口时的显示/隐藏边界，以及窗口时长改变时的 Dashboard 状态签名；本地构建版本升级为 `0.1.16-local.55`。

## 0.1.16-local.54（2026-08-03）

### 修复

- 修复“启动额度倒计时”请求被 ChatGPT Codex 后端以 `400 Stream must be set to true` 拒绝的问题。请求现强制使用 `stream:true` 和 `text/event-stream`，并解析 `response.completed`、`error`、失败、取消及不完整终态。
- 流在没有成功终态时会明确失败，不会把仅建立连接或中途断开的请求误报为倒计时已经启动。

### 验证说明

- 新增 SSE 成功、流内错误和缺失完成事件测试；本地构建版本升级为 `0.1.16-local.54`。

## 0.1.16-local.53（2026-08-03）

### 本地定制变更

- 保存账号卡片在普通额度刷新按钮旁新增“启动额度倒计时”按钮。它只对已识别且仍为 100% 的 5 小时/7 天窗口显示；任一倒计时低于 `4h55m` / `6d23h55m` 后自动隐藏。
- 点击后先强制刷新账号凭据和额度，再使用该账号在后台发送固定短消息 `Hi`；请求关闭工具、采用低 reasoning、设置 `store:false`，完成后再次强制刷新额度，不切换当前账号或创建 Manager 可见会话。
- 同账号并发点击会复用同一任务；成功后在扩展宿主内抑制按钮十分钟，覆盖新倒计时尚未越过五分钟显隐阈值的间隙，避免重复发送。

### 验证说明

- 覆盖 5 小时/7 天精确阈值、已消费额度、未知窗口、动态轻量模型选择、无工具请求体、账号 header、上游错误、Dashboard 动作分发、按钮位置和状态签名。
- 本地构建版本升级为 `0.1.16-local.53`，继续基于上游 `0.1.16`。

## 独立 Sub2API 导入器 0.1.3 与 Gateway 0.1.2（2026-07-27）

### 修复

- S+ 导入器识别新账号时改为以源会话提供的身份字段作为匹配子集。Sub2API 在导入时补充 `user_id`、组织等字段后，不再误判为找不到新账号；后置步骤会明确设置 `default` 代理、并发 `2` 和全部当前模型映射，并保留服务端补充的凭据字段。
- Gateway 保存与读取下游密钥时会去除可选的 `Bearer ` 前缀，避免形成双重 Authorization 方案。下游 `/v1/models` 返回 `401` / `403` 时，卡片会明确提示需要普通 `/v1` API Key，管理端登录令牌不能用于下游调用。

### 验证说明

- 覆盖服务端补充身份字段后的 S+ 唯一匹配、代理/并发/模型回读、带 `Bearer ` 前缀的下游密钥以及下游拒绝密钥的诊断提示。

## 独立 Sub2API 导入器 0.1.2（2026-07-26）

### 修复

- 恢复原私聊 S+ 路径的导入后配置语义，并将其保留在独立导入器中：仅对可唯一识别的本次新建账号绑定激活的 `default` 代理、`test` 分组、并发 `2`，并写入该分组全部当前候选模型的精确映射。
- 导入前验证目标代理、分组、账号身份与模型候选；导入后逐项回读。若导入已完成但配置无法确认，任务失败关闭且绝不自动重放导入。
- 支持可选的 Sub2API 管理端 refresh token。access token 过期时只刷新并重试一次；轮换后的 refresh token 保存在该独立组件的 owner-only 私有状态文件，服务重启后仍可继续使用。

### 验证说明

- 覆盖过期 access token 刷新、轮换 refresh token 的私有持久化、缺少 refresh token 的失败关闭、默认代理/分组/并发/全模型回写与回读、配置前拒绝和导入后无法唯一匹配时不重放。

## 独立 Sub2API Gateway 0.1.1（2026-07-26）

### 修复

- 将可选 `inventoryObserver` 的配置校验与下游 Gateway 主配置分离。观察器地址、分组、刷新周期或密钥引用不合法时，只读观察功能会安全停用并显示警告；下游地址、模型和密钥保存/启用流程保持可用。
- Gateway 主配置整体无效时，卡片仍保留“刷新”和“打开配置”操作，核心 Manager 的账号管理、配额与无感切号不会受该可选集成影响。

### 验证说明

- 覆盖错误观察器仍保留下游 Gateway 配置、可继续保存下游密钥和刷新，以及原有严格配置校验与只读观察测试。

## 0.1.16-local.52（2026-07-25）

### 本地定制变更

- 将飞书私聊 M+/S+ 机器人、Sub2API Gateway VSIX 和 S+ 管理端导入器拆分为独立可安装产物。核心 Manager 只保留版本化的通用集成 API、动态 Dashboard 卡片和供应商无关的 Gateway 生命周期；未安装可选包时不读取其配置、凭据或启动其运行时。
- Gateway 的配置、SecretStorage、只读库存观察、用量观察与显式失败关闭回退都迁至独立 VSIX；S+ 只有在独立导入器被显式配置并运行后才会消费私有队列任务。旧的内置配置不会被自动迁移、复制或删除。
- `sub2api-importer` 更新为 `0.1.1`：新增只提取旧 Sub2API 管理信息的私有环境迁移脚本与独立用户服务模板。已有飞书长连接“监控操纵助手”可保持同一 App 和群聊店铺查询，并将私聊 M+/S+ 适配到新队列；不再为同一 App 并行启动第二个入站机器人。
- runtime 协议升级至 v10；私聊机器人和 Manager 统一支持 `MANAGER_IMPORT_QUEUE_DIR`，旧变量仅保留兼容用途。
- 将自动无感调度、手动切号、外部 `auth.json` 收敛和 Sub2API → ChatGPT 回退的最终 app-server 身份交接收敛到同一条可续租跨宿主事务；Gateway 活跃时普通自动 OAuth 切换会被安全抑制。
- runtime 协议升级至 v9：每笔受协调器提交的切换携带短期 `operationId`，shim 仅保留有界、无凭据的 pending/switching/终态记录。控制 socket 超时或断开后，管理端在保持事务租约时回查终态；shim 重启导致结果不可确认时明确要求 reload，不会并发发起第二笔切换。
- 无受管旧账号时的回滚快照不再因不确定超时被立即删除；只在不确定期最多保留十分钟，成功、确定失败或实际完成回滚后立即清除。
- 飞书本地收件箱在远端资料请求和额度刷新前先将导入或覆盖的账号隔离出无感池并同步持久化；替换已入池账号时，旧凭据不再能在验证窗口内被选中。
- Gateway 回退与普通无感候选在额度相同情况下统一使用 `Free/K12 → Plus → Pro` 的套餐次级偏好；移除了仅供旧生产路径使用的直连 runtime helper。

### 验证说明

- 覆盖共享租约续租、socket 不确定事务的终态回查、未托管快照迟到回滚、Gateway 回退排序，以及飞书覆盖入池账号时先隔离后验证。

## 0.1.16-local.51（2026-07-25）

### 本地定制变更

- 低额度与耗尽后的候选统一按真实额度窗口排序：先选五小时额度更高的 `windowed` 账号；没有合格五小时窗口时再选周额度更高的 `reserve`。`reserve` 明确表示“没有五小时窗口”，不再被当作五小时额度为零。
- Free/K12 `reserve` 账号现在可以承接 Free/K12 的低额度/耗尽切换；只要源账号属于 Free/K12，所有 Free/K12 候选都要求最近两分钟内成功刷新，但不会为此强制刷新。
- 额度完全相同才以套餐作为次级偏好：`Free/K12 → Plus → Pro`，其后保持既有的重置时间、最久未使用和稳定账号 ID 规则。
- `planType` 现在将 `K12`、`K12 Plan`、`ChatGPT K-12` 和 `ChatGPT K-12 Plan` 识别为 Free/K12 类。

### 验证说明

- 覆盖 Free/K12 的两分钟新鲜度、Free reserve 周额度回退、五小时额度优先于套餐、同额度套餐偏好和阈值为 `0` 的全部会话耗尽路径。

## 0.1.16-local.49（2026-07-24）

### 本地定制变更

- 无感切号设置重排为“分档切号”“低额度切号”“切换策略”三个二级项；分档方式与等待时间、低额度阈值分别作为对应子项，说明文本同步精简。
- 新增独立的 `codexAccounts.seamlessSwitchLowQuotaEnabled`。关闭后，低额度、结构化 `usageLimitExceeded` 与耗尽批次不再发起新的自动切换；分档切号保持独立，已开始的事务允许完成。
- 该新开关首次未保存时继承旧分档开关状态，避免升级改变既有行为；保存后两个开关完全独立。
- runtime 协议升级至 v8：关闭或重新开启低额度切号会清除 shim 中的全局耗尽批次与近期恢复记录，防止旧信号在重新开启后重放；已在进行的切换保留自己的恢复快照。

### 验证说明

- 覆盖分档与低额度开关互相独立、关闭后的阈值/结构化耗尽无操作、shim 观察状态清空与重新开启后的新信号，以及设置布局与配置兼容读取。

## 0.1.16-local.48（2026-07-24）

### 本地定制变更

- 将无感切号阈值的 `0` 选项从“关闭”改为“耗尽后切换”。额度分档均衡继续生效：跨档仍可切换；同档内只在当前活动会话批次全部以结构化额度耗尽终止后才切。
- 新增有界的耗尽批次观察：从第一个额度终止信号起最多等待 6 小时；正常完成、手动中断、非额度失败或新工作会取消该批次，但不会清除既有的近期恢复记录。
- 将近期额度耗尽 thread 的恢复窗口扩为 6 小时。耗尽批次只决定是否切换，恢复仍复用既有事务；“耗尽后切换”下的跨档切换也会恢复刷新滞后造成的已终止会话。
- 非零低额度切换不再强制覆盖普通会话策略为自动继续。普通会话策略现在明确适用于额度分档、低额度和耗尽后切号；分档等待时间在设置页归入“额度分档切号”，低额度/耗尽设置单独分组。
- runtime 协议升级至 v7，状态只新增有界的耗尽批次就绪标记与单调批次号，不暴露 thread 内容。

### 验证说明

- 覆盖多会话全部额度终止、正常 peer 完成时取消批次但仍保留恢复、阈值为 `0` 的 monitor 选择、`26% → 1%` 跨 25% 分档、`24% → 1%` 同档保持，以及 Dashboard 分组与设置文案。
- 本地构建版本升级为 `0.1.16-local.48`，继续基于上游 `0.1.16`。

## 0.1.16-local.47（2026-07-23）

### 本地定制变更

- 精简中英文 README：将首页收敛为安装入口、功能/依赖分层、无感切号快速启用和详细文档索引，避免在首页重复完整技术规则。
- 明确区分本 fork 的 `-local` VSIX 与上游 Marketplace 发布；源码地址与包元数据改为本 fork，避免把本地增强误认为上游默认功能。
- VSIX 现在包含 `docs/` 与英文 README，因此安装后的 README 相对链接可直接打开详细的无感切号、Sub2API、飞书收件箱、更新与变更记录文档。

### 验证说明

- 本地构建版本升级为 `0.1.16-local.47`，继续基于上游 `0.1.16`。

## 0.1.16-local.46（2026-07-23）

### 本地定制变更

- 将原先独立的储备账号阈值、Free 1% 耗尽保护和结构化 `usageLimitExceeded` 紧急路径收敛为一个“账号切换阈值”：关闭、`1%`、`3%`（默认）或 `5%`。非零时所有 ChatGPT 源账号在相关五小时/周窗口达到阈值或收到结构化额度耗尽时，都会立即以中断并自动续接的方式切走；目标相关窗口必须严格高于该阈值。
- Free 账号继续不参与普通额度分档，但在统一阈值路径中先选择最近两分钟内刷新的安全 Free 同类账号；没有合格同类目标时才回到 Plus/Pro/储备的混合池。关闭阈值只关闭低额度与 runtime 耗尽硬切，普通额度分档仍保留。
- Dashboard 删除独立的“1% 耗尽保护”开关和旧的 `1%/2%/3%` 储备阈值，改为一个四档分段控件。旧显式设置在用户保存新设置前兼容读取：旧耗尽保护映射为 `1%`，旧 `2%` 储备值映射为 `3%`。
- Sub2API → ChatGPT Auth 回退只读取同一安全阈值筛选候选，不改写普通 ChatGPT 调度配置。

### 验证说明

- 覆盖统一阈值的 `0/1/3/5` 归一化与旧设置迁移、Free 优先与混合回退、首次低额度观测、周窗口、结构化 runtime 错误和关闭阈值后的无操作路径。
- 本地构建版本升级为 `0.1.16-local.46`，继续基于上游 `0.1.16`。

## 0.1.16-local.45（2026-07-23）

### 本地定制变更

- Sub2API → ChatGPT Auth 自动回退在真正交接前会强制刷新当前最优候选并重新排序；若刷新后另一候选成为最优，也会先刷新它。刷新失败、失去资格或缺少凭据的账号只在当前回退事务中排除，不会用旧额度快照继续切换。
- 回退未完成时由固定 5 秒重试改为 `5 → 10 → 20 → 40 → 60` 秒封顶的指数退避。Gateway 卡片会显示重试等待与次数；新额度耗尽事件会重置退避，成功事务则抑制已处理事件的重放。
- 普通 ChatGPT 无感分档、A/B/C、Free/Plus/Pro Dashboard 显示筛选及官方自动切号均未改变。

### 验证说明

- 回归覆盖候选强制刷新后的重新排序、刷新失败排除，以及失败回退的 5 秒/10 秒指数退避与状态提示。
- 本地构建版本升级为 `0.1.16-local.45`，继续基于上游 `0.1.16`。

## 0.1.16-local.44（2026-07-23）

### 本地定制变更

- Saved Accounts 右上角新增 `Free` / `Plus` / `Pro` 多选套餐筛选。不选套餐时不限制；选中一个或多个时，根据账号原始 `planType` 匹配，并与现有 `A/B/C` 可见性、隐藏账号显示取交集。如 `team` 或未知套餐等非三类账号在有套餐筛选时不会匹配，取消所有套餐筛选后仍会显示。
- 套餐筛选是 Webview 当前会话的本地显示状态，不写入 VS Code 配置，不改变无感切号池、后台配额刷新或 `A/B/C` 分组。当前页刷新、分页和批量选择遵从筛选后的显示范围。

### 验证说明

- 回归覆盖 Free/Pro 多选与分组、隐藏显示的交集，以及无套餐选择时保留原显示范围；Dashboard 签名同时包含原始套餐类型。
- 本地构建版本升级为 `0.1.16-local.44`，继续基于上游 `0.1.16`。

## 0.1.16-local.43（2026-07-23）

### 本地定制变更

- Sub2API Gateway 新增默认关闭的 `autoFallbackToChatGpt`：只有下游 `/v1/responses` 返回 `429`/`502`/`503` 且受限错误体可明确匹配额度耗尽语义时，才会从符合现有无感池新鲜度与储备规则的 ChatGPT Auth 账号中选择一个做单向、无 reload 回退。普通 5xx、网络错误、超时、模型错误或未识别响应不会误触发。
- 回退复用同一条本地 HTTP provider，本地 thread history 不因传输切换分裂；事务同时校验 app-server 身份、切换 relay 路由并提交 Manager 当前账号。任一步失败都会恢复原有身份与 Gateway 路由，不会留下部分切换状态。
- 为避免重复执行非幂等工具调用，已收到额度耗尽错误的当轮请求不会自动重放；切换完成后可在同一 thread 重试或继续。不会从 ChatGPT Auth 反向自动切回 Gateway。

### 验证说明

- 新增 Gateway 回退候选筛选、耗尽语义识别、成功回退与本地提交失败回滚的回归测试；并保留所有已有 Gateway 与无感切号路径。
- 本地构建版本升级为 `0.1.16-local.43`，继续基于上游 `0.1.16`。

## 0.1.16-local.42（2026-07-22）

### 本地定制变更

- 修复 Gateway adapter 的回环代理路由：仅对 Gateway 启动的 Codex app-server 子进程补充 `NO_PROXY`/`no_proxy` 中的 `127.0.0.1`、`localhost` 与 `::1`。这样随机回环 adapter 端口不会被继承的 HTTP(S)/ALL 代理截走；外部代理路由和父进程环境保持不变。

### 验证说明

- 使用实际 Codex app-server 与隔离回环服务复现：设置代理且没有 `NO_PROXY` 时请求不会到达本地服务；加入回环 bypass 后请求立即到达。
- shim 回归断言 Gateway 子进程得到回环 bypass，不打印任何代理值或凭据。
- 本地构建版本升级为 `0.1.16-local.42`，继续基于上游 `0.1.16`。

## 0.1.16-local.41（2026-07-22）

### 本地定制变更

- 修复 Gateway 重载启动竞态：回环 adapter 已就绪但 Manager 尚未从 SecretStorage 通过本地控制 socket 注入下游 Key 时，首个带有效本地令牌的请求会最多等待 15 秒，而不是立即以未就绪凭据失败。超时会留下安全的 `CREDENTIAL_TIMEOUT` 本地 `503` 诊断。
- Gateway shim 增加脱敏生命周期日志：adapter 就绪、凭据已配置、请求等待凭据、开始转发、客户端取消和已有的转发失败。日志仍不含 Key、Authorization、正文、完整 URL、响应文本或上游账号信息。

### 验证说明

- shim 回归覆盖请求先到、凭据后到时的等待与成功转发，并断言生命周期日志不含下游 Key 或请求正文。
- 以隔离的真实 Codex app-server 探针确认 `requires_openai_auth=false` 加 `env_key` 会将每进程本地令牌作为 Bearer 请求头发送给自定义 provider。
- 本地构建版本升级为 `0.1.16-local.41`，继续基于上游 `0.1.16`。

## 0.1.16-local.40（2026-07-22）

### 本地定制变更

- 修复最早本地 Gateway 版本创建的 thread 在恢复时找不到 `codex-accounts-sub2api` provider、因而在到达回环 adapter 前失败的问题。旧 ID 现在是内部兼容别名，始终指向当前手动选择的 ChatGPT Auth 或 Sub2API Gateway 传输；新 thread 继续使用统一 HTTP provider identity。
- Gateway 每次实际转发失败都会额外写一条 `Sub2API Gateway forwarding failed:` 的安全日志，包含来源、状态、transport code 和脱敏请求形态，便于直接在 Codex 输出/扩展日志中排查。

### 验证说明

- shim 回归覆盖普通与 Gateway 两种启动参数中的旧 provider 兼容别名，以及上游 502/本地连接失败的固定格式日志；断言日志不包含下游 Key 或请求正文。
- 本地构建版本升级为 `0.1.16-local.40`，继续基于上游 `0.1.16`。

## 0.1.16-local.39（2026-07-22）

### 本地定制变更

- Sub2API Gateway 在实际转发失败的瞬间同步保存一条固定大小、owner-only 的诊断记录。即使随后必须切回 ChatGPT Auth 才能继续对话，Dashboard 仍可显示最近失败来源、HTTP 状态、Node transport code 与脱敏请求形态。
- 诊断不保存请求正文、API Key、Authorization、完整 URL、响应文本或上游账号信息；解析时也只接受白名单字段，避免本地文件内容被原样反射到 Dashboard。

### 验证说明

- shim 端到端测试覆盖 Sub2API HTTP 失败与本地 `ECONNREFUSED`，并断言两类诊断均不含下游 Key 或请求正文；Controller 回归覆盖切回 ChatGPT Auth 后读取该记录。
- 本地构建版本升级为 `0.1.16-local.39`，继续基于上游 `0.1.16`。

## 0.1.16-local.38（2026-07-22）

### 本地定制变更

- Sub2API Gateway 卡片改为额度/使用视图：有明确上游观察数据时展示动态聚合的 5 小时与每周上游池；没有可读窗口时展示近 5 小时、近 7 天及今日的 Gateway 已观察 token，不再显示容易误解的请求/成功/失败计数或伪造额度百分比。
- 新增可选 `inventoryObserver` 配置块和独立 SecretStorage 引用。它只在用户明确配置并保存独立管理观察密钥后，以只读 GET 聚合指定分组的可调度 OpenAI 上游账号 primary/secondary 额度；账号库存增减会在下一次刷新中更新容量分母，读取失败的账号不会被记为 0%。
- Gateway token 只从本地回环适配器收到的最终 Responses `usage` 汇总中聚合，使用独立的五分钟匿名 token 桶和独立状态键；不会混入普通 ChatGPT 本机用量、账号窗口统计或无感切号池。
- runtime protocol 升级到 v5。Gateway 与 ChatGPT HTTP transport 复用本地 provider identity，避免只因切换端点而将本地 thread history 按 provider 分开；仍需显式切换和 reload，且不会让两种上游服务共享远端上下文或自动切号。
- Gateway 状态会区分本地适配器无法连接与 Sub2API 已返回的上游 HTTP 失败，便于定位 `502`；不显示请求正文或凭据。

### 验证说明

- 覆盖观察器配置/明文管理 Key 拒绝、分组账号窗口聚合、不可读取账号不伪造 0%、独立 SecretStorage、Gateway token 滚动窗口、Dashboard 签名与 provider identity。
- shim 端到端测试覆盖未注入 Key 的 503、真实 Key 仅经回环转发、SSE `response.completed` token 汇总，以及 Sub2API 返回 502 时的来源标记。
- 本地构建版本升级为 `0.1.16-local.38`，继续基于上游 `0.1.16`。

## 0.1.16-local.37（2026-07-22）

### 本地定制变更

- 新增默认关闭的本地 Sub2API Gateway：一个 Gateway 卡片对应一个下游 API，不伪造 OAuth 账号，也不会进入 ChatGPT Auth 的五小时额度或无感切号池。
- Gateway 配置文件只接受扩展 global storage 下的相对路径；明文 API Key 配置会被拒绝，真实下游 Key 仅保存在 VS Code SecretStorage。
- 选中 Gateway 后，runtime 启动仅监听 `127.0.0.1` 的每进程适配器，使用随机本地令牌接收 Codex 请求，并在内存中替换为 SecretStorage Key 后转发到配置的 Sub2API `/v1`。
- 卡片展示当前 `/v1/models` 健康快照与 Manager 观察到的请求/成功/失败计数；库存或模型变化不会伪造、重置或换算成五小时额度。

### 验证说明

- 覆盖相对配置路径、模板创建、明文密钥拒绝、SecretStorage 注入、跨 runtime 观察计数，以及未注入 Key 时适配器 503、注入后仅向 Sub2API 转发真实 Key 的端到端 shim 测试。
- 本地构建版本升级为 `0.1.16-local.37`，继续基于上游 `0.1.16`。

## 0.1.16-local.36（2026-07-22）

### 本地定制变更

- 修复账号卡片的当前额度窗口 Token 统计被相邻 `reset_at` 秒级漂移拆分的问题：当前账号额度边界一分钟内的受管用量会合并显示，切换账号只累计对应账号。
- 账号窗口统计独立检查最长 31 天的受管元数据，覆盖现有 30 天长期额度窗口；它不受本机用量 Dashboard 的 14 天保留范围截断。
- 真正的额度 reset 边界变化仍会立即舍弃旧桶，归零后等待该账号下一次受管使用。

### 验证说明

- 新增覆盖长周期窗口、14 天本机面板裁剪、秒级 reset 边界漂移合并和真实新边界不混入旧统计的回归测试。
- 本地构建版本升级为 `0.1.16-local.36`，继续基于上游 `0.1.16`。

## 0.1.16-local.35（2026-07-22）

### 本地定制变更

- 本机 Codex 用量的 `24h` 视图改为“今日”：按本地时区当天 `00:00` 起的固定 `3` 小时分档聚合，不再随刷新时间滚动；未来尚未开始的分档不显示，当前分档持续累计，缓存会在本地午夜立即过期并重算。
- “本机 Codex 用量”标题行新增“刷新用量”按钮，可立即强制执行一次 token 汇总并更新 Dashboard 数值；它仍复用跨宿主扫描租约，不会并发重复扫描。
- 本地用量缓存升级到 v5，避免升级后短暂复用旧的滚动 24 小时聚合。

### 验证说明

- 覆盖本地时区固定边界、未来分档隐藏、当前分档归属、手动刷新绕过 15 分钟缓存、Dashboard 动作链路和窄窗口布局。
- 本地构建版本升级为 `0.1.16-local.35`，继续基于上游 `0.1.16`。

## 0.1.16-local.34（2026-07-22）

### 本地定制变更

- 修复无五小时窗口的 Plus/长周期账号不显示 Token 统计：当 runtime 只提供一个 `primary` 额度窗口时，现在按 `window_minutes` 判断短期或长期，而不再固定归为五小时。

### 验证说明

- 新增单一 43,200 分钟 `primary` 窗口归入长期额度桶的回归测试。
- 本地构建版本升级为 `0.1.16-local.34`，继续基于上游 `0.1.16`。

## 0.1.16-local.33（2026-07-22）

### 本地定制变更

- 每张带有可识别五小时或周额度窗口的账号卡片可显示“当前窗口 Token”、输入、输出与缓存小计；统计与当前额度 reset 时间精确匹配，新窗口不会混入旧额度数据。
- 无感 runtime protocol 升级至 v4：仅在受管 turn 开始时批量记录不透明本地账号 ID、thread ID 与时间，不记录会话正文、提示词、邮箱、远端账号 ID 或凭据。
- 统计复用原有 15 分钟流式 token 扫描，归因 journal 读取总量上限 2 MiB；无逐 token IPC、额外网络请求或会话正文二次加载。安装前历史不回填，第一个受管 turn 后开始累计。

### 验证说明

- 覆盖账号切换归因、五小时/周窗口 reset 分桶、重置边界归零、卡片渲染签名与 runtime v4 协议测试。
- 本地构建版本升级为 `0.1.16-local.33`，继续基于上游 `0.1.16`。

## 0.1.16-local.32（2026-07-22）

### 本地定制变更

- Saved Accounts 改为每页最多 `50` 张卡片；隐藏、解除隐藏及 `A/B/C` 分组显示变化会立即重分页，页码超出范围时自动夹回有效页。
- Dashboard 顶部操作明确改为“刷新当前页配额”，只请求当前显示页。低周额度一键隐藏也跟随当前页，避免一次操作遍历大量非可见卡片。
- 定时配额刷新改为只处理未隐藏、已启用分组的稳定第 `1` 页（最多 `50` 个账号）；归档账号保留单卡、选择批量和命令面板的人工刷新路径。
- 批量配额刷新合并重置额度回写的视图通知，整批结束后只发布一次 Dashboard 更新，避免每个账号各触发一次完整快照。

### 验证说明

- 覆盖 101 条账号的分页、删除后页码夹位、隐藏/禁用分组排除、首屏 `50` 条自动刷新范围、Dashboard 当前页刷新参数，以及批量刷新单次视图更新。
- 本地构建版本升级为 `0.1.16-local.32`，继续基于上游 `0.1.16`。

## 0.1.16-local.31（2026-07-22）

### 本地定制变更

- Saved Accounts 右上角新增“解除隐藏周额度 >90%”一键恢复操作。它严格选择已隐藏、已识别周额度且高于 `90%` 的账号，不受当前分组显示状态影响。
- 该恢复操作在一次账号索引写入中解除隐藏、重新加入无感切号池并清空 `A/B/C` 分组；普通批量“解除隐藏”仍保留原分组。

### 验证说明

- 覆盖 `90.01%` / `90%` 边界、隐藏状态、无周窗口过滤，以及存储层和 Dashboard 操作层的解除隐藏、入池、清空分组行为。
- 本地构建版本升级为 `0.1.16-local.31`，继续基于上游 `0.1.16`。

## 0.1.16-local.30（2026-07-21）

### 本地定制变更

- 修复批量隐藏成功后账号卡片仍保留勾选的问题：仅在宿主确认隐藏成功后，才从当前选择中移除实际被隐藏的账号；失败时保留原选择，不会误清除等待期间新增的勾选。
- Saved Accounts 右上角新增“隐藏周额度 <3%”一键操作。它严格只作用于当前显示范围内、已识别周额度且低于 `3%` 的未隐藏账号，因而遵循分组筛选，并不会把没有周额度窗口的账号误隐藏。

### 验证说明

- 覆盖隐藏操作的成功回传、选择状态清理，以及 `2.99%`、`3%`、已隐藏和未识别周窗口账号的批量目标过滤。
- 本地构建版本升级为 `0.1.16-local.30`，继续基于上游 `0.1.16`。

## 0.1.16-local.29（2026-07-21）

### 本地定制变更

- 新增持久化账号分组：可批量将账号放入 `A`、`B`、`C` 或移出分组，卡片显示分组标签；面板右上角分组按钮同时控制分组显示和无感候选范围。
- 未分组且未隐藏账号始终显示并参与无感候选；隐藏状态优先。关闭某分组不会改动池成员、手动切号或官方自动切号，已激活账号也不会被立即强制切走；在其后续原有触发条件满足时，只会切往当前显示范围的候选。
- Dashboard 的“一键刷新配额”改为只刷新当前可见账号，避免隐藏账号或已关闭分组账号在筛选状态下被额外请求。

### 验证说明

- 覆盖账号分组持久化、隐藏后保留分组、Dashboard 操作、设置持久化、状态签名、分组候选过滤和当前可见账号刷新路径的回归测试。
- 本地构建版本升级为 `0.1.16-local.29`，继续基于上游 `0.1.16`。

## 0.1.16-local.28（2026-07-21）

### 本地定制变更

- 新增持久化的账号隐藏状态：卡片左上角可批量隐藏/解除隐藏，面板眼睛按钮可切换显示，隐藏卡片采用紫色标识。
- 隐藏时自动移出无感切号池，并从无感分档、Free 1% 保护、原有自动切号和手动/runtime 切换中排除；解除隐藏后自动重新加入无感切号池。

### 验证说明

- 覆盖账号存储、跨刷新签名、无感候选、原有自动切号、runtime 拦截、Dashboard 操作和面板布局的回归测试。
- 本地构建版本升级为 `0.1.16-local.28`，继续基于上游 `0.1.16`。

## 0.1.16-local.27（2026-07-21）

### 本地定制变更

- 将无感切号的可选 `1%` 保护调整为 Free 优先：经实际有效五小时/周窗口校验的 Free 账号在五小时额度到 `1%` 或以下时，优先切到池中最近两分钟内刷新、五小时额度最高且周额度仍安全的 Free 账号；没有合格 Free 时才回到原有混合 windowed/reserve 选择。
- 将 runtime 结构化 `usageLimitExceeded` 接入同一选择器：无需等待下一次配额刷新，受限普通会话会以中断后 Continue 的既有语义恢复，Goal 继续使用暂停/恢复路径；远端窗口若已落后于共享活动账号会直接收敛，不重复选号。
- 新增只在该开关开启时运行的轻量 runtime 监测器：每两秒读取固定大小状态 JSON，不读取 thread ID、会话正文或历史，不保存增长型缓存；选择失败 10 秒退避，状态 RPC 失败 30 秒退避，并以共享 `seamless-switch` 租约避免多宿主重复决策。

### 验证说明

- 覆盖 Free 最高额度优先、Free 候选的 1%/周额度/两分钟新鲜度排除、无 Free 时混合回退、运行时限额事件、跨窗口收敛、轮询关闭与退避的回归测试。
- 本地构建版本升级为 `0.1.16-local.27`，继续基于上游 `0.1.16`。

## 0.1.16-local.26（2026-07-20）

### 本地定制变更

- 修复本地用量把重复 `token_count` 上报反复累加的问题：改为按每个 rollout 的 `total_token_usage` 累计高水位计算增量，重复值和过期回退值不再增加统计。
- 修复 multi-agent spawned subagent 复制父会话历史导致的巨额重复计数：首个 `session_meta` 标记为子代理时，复制前缀只建立累计基线，从子代理自己的 `trigger_turn` 边界后才计入新增。
- 排除晚于扫描快照时间的事件，并升级本地聚合缓存 schema/文件名，使旧的错误缓存不会继续展示 15 分钟。

### 验证说明

- 新增累计重复、过期回退、跨日基线、未来事件和子代理继承历史回归测试。
- 以 2026-07-20 17:09:50（Asia/Shanghai）为固定截点扫描真实会话集，扩展统计得到 `291,687,461` token；独立 `jq`/`awk` 高水位与子代理边界计算在 input、cached input、output、reasoning output、total 和 event count 上逐项一致。
- 本地构建版本升级为 `0.1.16-local.26`，继续基于上游 `0.1.16`。

## 0.1.16-local.25（2026-07-19）

### 本地定制变更

- 修复本地用量扫描导致 Remote Extension Host 内存持续增长并触发 OOM 重启的问题。
- 扫描前按保守 mtime 窗口排除不可能包含近 14 天事件的旧会话文件；流式读取时只解析 `turn_context` 与 `token_count`，不再为会话正文和工具输出分配 JSON 对象图。
- 复用时区日期格式器，避免每条 token 事件重复构造 `Intl.DateTimeFormat`；本地会话原始内容与缓存隐私边界保持不变。

### 验证说明

- 真实 331 MB 活动会话集在 512 MB Node 堆限制下完成扫描，峰值 RSS 约 100 MB，122 个来源文件的现有窗口统计保持可用。
- 本地构建版本升级为 `0.1.16-local.25`，继续基于上游 `0.1.16`。

## 0.1.16-local.24（2026-07-19）

### 本地定制变更

- 修复删除全部受管账号后的首次无感切换：从当前有效 `auth.json` 建立仅驻留内存的回滚快照，经 live app-server 身份校验后执行切换，失败时恢复旧身份。
- 强化 Mac、Windows 与 Remote-SSH 多宿主一致性：账号索引/token/配额镜像使用原子写入、共享租约、三方合并与 stat/revision 轮询，减少新导入账号、额度和活动账号被旧缓存覆盖的问题。
- 本地用量 Dashboard 会采用其他宿主写入的更新聚合，并在 `nextRefreshAt` 主动刷新；扫描租约可过期回收，缓存继续只保存匿名汇总。
- 无感分档新增 `1%`、`2%`、`3%` 储备阈值，并按真实额度窗口区分分档账号、无五小时窗口的储备账号和数据未知账号，不再依赖 Free/Plus 套餐标签猜测。
- 普通自动调度优先轮换分档账号，全部达到阈值后才启用周额度最高的储备账号；储备账号到达阈值时优先回到已恢复的分档账号，手动切号逻辑保持不变。
- 1% 紧急切换优先选择仍安全的储备账号，再回退到五小时额度高于 1% 的分档账号；普通会话 Continue、持久 Goal 暂停/恢复和 workspace 权限保持语义不变。
- 补充冷启动、多窗口收敛、混合账号选择、本地用量同步与隐私边界的回归测试和中英文文档。

### 构建说明

- 本地构建版本升级为 `0.1.16-local.24`，继续基于上游 `0.1.16`。
- 恢复 `.eslintrc.json` 兼容的 ESLint 8，并固定可在当前 Node 18 发布环境运行的 VSCE 2.32/兼容 Cheerio；运行时依赖不受影响。
- ESLint 固定在兼容 `.eslintrc.json` 的 8.57.1；VSCE/Cheerio 固定在支持当前 Node 18.19 构建宿主的版本，避免宽松依赖漂移导致 lint 或打包命令无法启动。

## 0.1.16（2026-07-13）

### 变更内容

- 新增默认关闭的 `5 小时配额控制`，关闭时仍展示配额，但不参与自动切号或低配额提醒。
- 自动化判断增加配额窗口存在性校验，避免接口缺失窗口被缺省值误判为 `0%`。
- 每周配额保持独立，可在关闭 5 小时自动化时继续触发切号与提醒。
- profile 与订阅刷新结合已保存的 workspace 名称和结构选择正确记录，并可回写修复历史错误的 `accountId`。
- 账号信息同步等待订阅刷新完成后再发布 Dashboard 状态，确保套餐与到期时间及时更新。
- 补充配额窗口、自动化开关、workspace 选择、`accountId` 自修复和订阅解析的回归测试。

### 说明

- 本版本公告使用新 `id`：`ann-2026-07-codex-tools-0-1-16`。
- 本次发版同时修复 5 小时配额误报与同邮箱 Personal / Team workspace 历史身份数据问题。

## 0.1.15（2026-07-08）

### 变更内容

- 修复同邮箱下个人 `Plus` 与团队 `Team` workspace 共享 `accountId` 时，profile/workspace 识别可能串到同一账号上下文的问题。
- 远端 `accounts/check` / profile 解析优先使用选中的 workspace 记录，并把 `organizationId` 纳入 profile 缓存键。
- 收紧 Aideck mirror token 吸收条件，仅在 `email`、`userId`、`accountId`、`organizationId` 以及 token claims 一致时才覆盖本地 SecretStorage。
- mirror 文件若出现“顶层 identity 与嵌入 token claims 不一致”，现在会在读取阶段直接丢弃。
- mirror 回写增加 identity 校验，并默认保留已有 workspace/profile/quota 字段，避免跨项目继续放大脏数据。
- 补充多 workspace、mirror identity 冲突和 token 回写安全的回归测试。

### 说明

- 本版本公告使用新 `id`：`ann-2026-07-codex-tools-0-1-15`。
- 本次发版同时包含 `codex-tools` 与 Aideck 串号问题对应的 VS Code 侧止血修复。

## 0.1.9（2026-04-28）

### 变更内容

- 打包体积优化：VSIX 不再包含 `node_modules` 中的构建期依赖。
- 排除 `lightningcss.darwin-arm64.node` 等构建阶段原生二进制，避免安装包被撑到约 4 MB。
- 0.1.9 VSIX 体积恢复到约 636 KB。
- `.vscodeignore` 新增 `node_modules/**`，后续打包默认只保留扩展运行需要的文件。
- 更新公告 `id` 与 `releaseVersion` 同步为 `0.1.9`。

### 说明

- 本版本公告使用新 `id`：`ann-2026-04-codex-tools-0-1-9-package-size-optimization`。
- 本次不改变运行时功能，主要修复发布包内容与体积。

## 0.1.8（2026-04-28）

### 变更内容

- 详情页主题跟随修复：`auto` 模式优先识别 VS Code 注入的 `vscode-light` / `vscode-dark` / `vscode-high-contrast`，不再只依赖系统深浅色。
- 详情页主题实时同步：监听 VS Code 主题 class 与系统浅色媒体变化，已打开详情页切换深浅色更稳定。
- Dashboard 主面板精简：移除配额卡片上方的“Codex Accounts Manager · 配额总览”说明栏。
- 更新公告规则优化：存在多个 `releaseVersion` 更新说明时，只保留最新版本公告展示。
- 发布记录归档：新增 `docs/CHANGELOG.md` 作为长期版本记录，`announcements.json` 只保留当前版本弹窗说明。
- README 同步：补齐当前功能、命令、设置项以及更新公告维护约定。

### 说明

- 本版本公告使用新 `id`：`ann-2026-04-codex-tools-0-1-8-theme-announcement-polish`。
- 0.1.7 的历史内容保留在本文件下方，不再放入当前版本弹窗公告。

## 0.1.7（2026-04-28）

### 变更内容

- 详情页主题策略优化：新增 `auto` 自动跟随主题，优化浅色/深色下的变量切换与样式细节。
- 详情页使用量 tip、状态标签、隐私入口按钮在浅色主题下对比度优化。
- Dashboard 打开详情页时同步当前隐私模式，支持首次直接隐藏敏感信息（邮箱、名称、ID）。
- 详情页新增订阅到期展示并修复个人 Pro 空间显示。
- Dashboard 更新弹窗新增 `announcement` 通知规则：未读提示可标记已读。
- 状态栏 tooltip 去除固定深色代码高亮背景，提升主题一致性。
- 账号卡片翻面增强：新增工作区、订阅有效期、添加方式、账号状态、user id 与标签查看。
- 额外模型额度展示覆盖更全，配额卡片排版更稳。
- 自动切号、后台刷新相关交互行为与性能路径改进。

### 说明

- 未来每次发版都在本文件追加新版本记录，不会删除历史说明。
- `announcements.json` 中仅保留最新版本说明用于弹窗提示，历史说明请从本文件追溯。

## 0.1.6（2026-04-27）

### 变更内容

- 增强配额刷新与账户状态同步链路。
- 改进卡片状态指示与错误提示。
- 补齐多个语言的细化提示文案。

## 0.1.5（2026-04-26）

### 变更内容

- 新增自动切号策略与告警。
- 新增共享 JSON 导入导出与账号恢复流程。
- 增加更多可读性与交互优化。
