# 账号状态：验收清单

本清单对应用户在 2026-09-14 确认的规则。不能把单元测试、模拟服务端、独立测试窗口或安装包一致性检查写成“用户日常面板实景通过”。没有证据的项目必须保留未验收。

## 最新配色：交换青蓝与黄色

已确认可用但续期不可用改为青蓝色；会话状态未知改为黄色（续期是否有结论仍分别说明）。仅交换配色，状态判定、中文标签、钥匙动作及其他警告不变。当前映射见 [状态机与界面表格](account-state-machine.md)。

本轮验收要求：组件检查两类配色与原账号钥匙绑定；卡片和详情页、深浅主题的样式映射一致；全量回归、类型检查和本地覆盖安装；只读核对真实账号分类没有因换色改变。实际面板与像素检查必须另行记录，不能复用换色前的人工确认。本轮结果记录在 [本地验收记录](account-state-verification.md)。

本轮结果：590 项测试通过，本地安装及 7 项文件一致性检查通过；18:06 实际面板全部 15 张卡片的状态类、中文标签和 11 把启用钥匙核对通过，映射为正常 4、青蓝 1、黄色未知 10。分组 B 筛选已恢复。实际像素颜色、其他主题／悬停，以及需宿主重启加载的详情页渲染未冒称实测通过。

以下带时间的小节及原始界面证据保留当时配色，均为历史记录，不能据其中“黄色 1、青蓝色 10”解释新版颜色。

## 17:43 重启后的实际面板验收（换色前历史）

- 新版实际于 17:29:28 激活。17:31 直接读取真实扩展数据库确认 15 个账号均已保存本轮检查标记，4 个成功证据及遗漏错误的修正已落盘；不再只是审计进程的离线迁移结果。
- 17:39:30 通过编辑器原生辅助功能读取了 **15 个真实账号卡片节点**，没有读取对话中引用的状态文字：正常 4、黄色 1、青蓝色 10、旧灰色标签 0；11 个“重新授权”按钮均存在且启用。原先隐藏分组 B 内的两个账号也纳入检查，随后已恢复原筛选。
- 原始脱敏界面证据：[account-state-live-ui-evidence.json](account-state-live-ui-evidence.json)。辅助功能确认文字、状态样式类及按钮存在，不等于截屏像素颜色检查。用户随后明确反馈“我看了，没问题”，记录为当前面板的人工目视确认，不扩大为深浅主题、悬停和认证流程均通过。
- 17:43 隐藏青蓝账号实际选中／取消选中，选中期间保留青蓝状态类及启用钥匙，原选择已恢复；证据：[account-state-live-selection-evidence.json](account-state-live-selection-evidence.json)。未执行批量动作、切号或重新授权。
- 17:37:50–17:37:51，3 个非当前账号由原有定时任务完成真实续期，新证据已写入本地存储且继续显示正常。未额外发起任何续期或模型探测，自动配额刷新仍为原设置 1 分钟，后台续期机制未更改。
- 17:44:08 另一个非当前正常账号也由原任务成功续期，因此本轮 4 个正常账号都已有新的真实续期成功记录。17:45:32 当前账号同一份成功证据已超过 16 分钟，实际卡片和后台判定仍为黄色。
- 持续只读观察已于 17:50:13 正常结束：17:32:13–17:50:13，共 1080 秒、37 次采样，退出码 0；全部采样均为正常 4、黄色 1、青蓝色 10。当前黄色账号始终使用同一份 17:29:02 的成功证据，最终证据年龄超过 21 分钟，未降级。目录：`/tmp/codex-runs/no-gray-live-persistence/20260914-173213-39627-7637/`。脚本只验证真实存储及安装代码，面板另由上面的原生界面证据与用户目视确认支撑。
- 只确认最新包的一次实际重启。用户“看了没问题”不等于确认第二次重启；第二次重启、真实失效账号以及重新授权取消／失败／错账号仍未实景验收。

## 17:23 修复轮次：旧灰色兜底属于缺陷（换色前历史）

- 用户截图确认：旧包仍有灰色“可用性待确认”，本项验收失败；之前把它解释成可保留状态是错误的。
- 查明 5 个旧灰色账号：4 个有成功续期记录及对应签发凭据，未接入新版证据；1 个收到 `invalid_refresh_token`，分类器遗漏了该明确错误码。
- 本轮必须消除独立灰色类别和原标签；未知统一青蓝色并有钥匙，但无续期结论时不得写成“续期不可用”。不能仅改颜色：还必须验证这 4 个历史成功账号恢复正常、漏判账号归入青蓝色续期不可用。
- 新增反例检查：只有成功时间、签发时间不符、账号不符、已到期、有较新拒绝、凭据更换后重复迁移，均不能生成可用证明；旧漏判只允许在原凭据和同次错误时间匹配时修复。
- 本轮修改前回归 5 项失败，修复后全量 59 个文件、582 项通过；类型检查通过，代码检查 0 错误、112 项警告。
- 17:27 新包覆盖安装成功，唯一注册版本 `0.1.19-dev`；安装目录关键文件与构建一致。安装目录代码加真实账号记录重放并断言：正常 4、黄色 1、青蓝色续期不可用 10，旧灰色 0。日志：`/tmp/codex-runs/no-gray-installed-accounts/20260914-172751-38743-9a6a/`。迁移只在审计进程内存演算，没有直接改写真实扩展数据库。
- 包 SHA-256：`ac5df37363dbd55852f879fbb4853eddb79654c62a5df019f27f8fdd76c5fc81`。已请求用户按原安排手动重启；本轮重启后面板尚未验收，不能把这次离线重放当作实际面板通过。

## 硬限制

- 不逐个切账号，不新增模型会话探测，不提高自动刷新频率。
- 配额结果不能替代会话认证证据；续期成功可直接提供正向证据。
- 黄色未知状态允许在没有新证据时保持，不暗示正在后台探测；青蓝色表示续期不可用但仍有有效可用证据。
- 原验收阶段仅本地安装，不提交、不推送；2026-09-14 用户在完成本地核对后另行授权推送至原 GitHub 分支。本次推送不包括服务器部署，也不扩大此前实景验收结论。
- 不撤销正常账号、不清空真实账号数据来制造测试结果。

## 功能与验收层级

本表按最新颜色定义；明确标记为历史的实景结果仅证明当时行为，不冒充本次换色的像素验收。

| 项目 | 必须满足 | 自动化验证 | 用户实际安装面板 |
| --- | --- | --- | --- |
| 正常 | 续期成功，无需会话记录 | 通过，模拟续期结果 | 4 个真实账号均在本轮由原定时任务再次真实续期成功并保持正常 |
| 青蓝色 | 续期不可用，仍有当前凭据的有效可用证据 | 状态测试通过；本轮配色检查见最新记录 | 历史 1 个当前账号，真实自然会话证据及状态一致；新版颜色另验 |
| 黄色未知 | 当前可用性未知，按续期是否有结论分别说明；不能冒充红或青蓝 | 状态测试通过；本轮配色检查见最新记录 | 历史 10 个真实账号，包含非当前、隐藏、分组 B；不声称其上游实际可用或不可用 |
| 红色 | 当前凭据真实认证拒绝且确认无法恢复 | 通过，模拟会话拒绝与恢复 | 待验收；没有真实失效样本不能虚报 |
| 网络与配额 | 不被当作认证失败或正向可用证明 | 通过，模拟错误与配额 | 待验收 |
| 黄转青蓝 | 同账号自然会话成功，续期失败仍保留 | 通过，模拟观察事件 | 待验收；不得补发模型探测 |
| 黄／青蓝转正常 | 同账号续期或重新认证成功，保存凭据后更新 | 通过，模拟续期与 OAuth | 待验收 |
| 青蓝转黄 | 有效可用证据随凭据到期或更新而失去适用性，且当前凭据续期已明确不可用 | 通过，快进时钟与换凭据 | 待验收 |
| 钥匙 | 当前、非当前、隐藏账号均有；动作绑定该账号 | 通过，实际组件动作绑定 | 11 张黄／青蓝卡片均有启用按钮；实际 OAuth 流程仍未执行 |
| 认证取消、失败、错账号 | 不清状态，不错误覆盖原账号 | 通过，模拟 OAuth 与实际命令服务 | 待验收，需要真实交互 |
| 配色 | 深浅主题、隐藏、选中、悬停仍可辨认青蓝／黄色，中文标签 | 本轮样式检查见最新记录；不等于像素验收 | 换色前面板用户确认没问题；不复用为新版颜色确认 |
| 两次重启 | 仍有效的证据及续期错误不丢失 | 通过，模拟两次重新加载存储 | 确认最新包 17:29 一次真实重启后恢复证据；尚不满足两次重启 |
| 超过十五分钟 | 不因为固定观察时长而降级，使用凭据实际到期时间 | 通过，快进时钟 | 历史 18 分钟、37 次采样分类稳定；当前账号同一份证据最终超过 21 分钟未降级，17:45 实际卡片已核对 |
| 全部账号 | 每个账号的结果和证据归属一致，包含非当前、隐藏账号 | 历史重放：正常 4、可用但续期不可用 1、续期不可用且会话未知 10，灰色 0；本轮重放见最新记录 | 历史 17:39 逐卡核对全部 15 个真实账号；无漏掉分组 B，原筛选已恢复 |
| 账号与凭据隔离 | 不接收其他账号、工作区、旧凭据或旧运行实例的新错误 | 通过，状态与观察处理测试 | 待验收 |
| 请求审计 | 验收额外模型探测数 = 0，验收切号数 = 0 | 观察脚本模型请求、切号、凭据写入均为 0 | 本轮未发送额外模型探测、切号或主动续期；只临时改变显示筛选及选中状态，均已恢复 |

## 历史数据迁移限制（本轮修正）

旧版的成功时间没有绑定凭据摘要，不能只凭该时间生成成功证据。完整本地续期记录的一次性衔接要求：续期开始／完成、当前凭据签发时间、账号身份及有效期必须同时匹配，且没有较新失败或新版证据；衔接后以摘要绑定并标记已检查，禁止把同一旧记录用于替换凭据。条件不足为黄色未知，不再产生灰色界面状态。

## 运行记录（16:47 轮次历史，非最新结论）

- 修改前：新增回归测试 8 项失败，复现没有青蓝配色、没有本地证据恢复入口。
- 全量自动化：2026-09-14 16:48，58 个测试文件、567 项测试通过。日志：`/tmp/codex-runs/cyan-state-final-tests/20260914-164826-31700-bec6/output.log`。
- 静态检查：类型检查与构建通过；完整检查 0 错误、105 警告（尚未进行无关格式化清理）。
- 本地安装：`0.1.19-dev`，16:47 强制覆盖安装成功；扩展索引仅一份注册。安装目录的后端、入口、面板脚本、两份样式、详情页和状态机均与打包源一致。
- 包 SHA-256：`66ce666c0c7d3ac8068fbdf583bec863151d65ff0fc6e4128ffa1d0232dff379`。
- 只读观察：`/tmp/codex-runs/cyan-installed-observation/20260914-164807-31657-9abb/`。16:48:07–17:06:07，共 1080 秒、37 次采样，退出码 0。开始时 15 个灰色类别；16:53:37 读到 10 个青蓝类别和 5 个灰色类别，此后至结束未变化。观察对象是已安装判定器加真实本地存储，不是正在显示的面板，也不证明账号上游实际可用。全程没有有效正向样本，且新分类只连续观察了 12 分 30 秒；不能把本次总时长写成“黄色真实保持超过十五分钟通过”。
- 用户手动重启后继续验收：编辑器日志记录 16:53:39 启动扩展宿主、16:53:41 激活本扩展；尚未观察到第二次真实重启。17:05 再次核对，唯一注册版本仍为 `0.1.19-dev`，安装目录 10 项关键文件仍与本次构建一致。
- 17:03 的真实本地存储快照：10 个 `refresh_unavailable_unverified`、5 个 `unverified`；所有账号均没有当前凭据的正向可用证据，不能据此声称绿色、黄色、红色或黄转青蓝实景通过。10 个青蓝类别包含当前账号、2 个可见非当前账号、7 个隐藏账号；5 个灰色类别均为隐藏账号。
- 原生辅助功能能读到编辑器及状态栏，但尝试打开账号面板后未能读取到对应面板；截屏失败，浏览器连接也不可用。没有把工具返回“按下成功”视为面板已打开，亦没有把对话里引用的账号状态文字当作面板内容。已向用户请求当前面板截图；真实配色、文字和钥匙仍未验收。
- 本轮没有代替用户重启，没有发起重新认证，没有新增模型探测、切号或主动续期；没有修改业务代码、重新安装、提交或推送。辅助功能临时开关已恢复原值。
- **当前阶段：代码与本地安装已完成，完整验收未完成。不能宣称整个任务已完成。**
